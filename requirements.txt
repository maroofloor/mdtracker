# --- UI ---
PySide6>=6.6
qtawesome>=1.3

# --- 화면 캡처 + OCR ---
mss>=9.0
Pillow>=10.0
pytesseract>=0.3.10        # tesseract 바이너리 별도 설치 필요 (kor 학습데이터)
numpy>=1.24               # OCR 결과 템플릿 매칭(마스크 IoU)

# --- 차트 ---
pyqtgraph>=0.13

# --- 엑셀 내보내기/가져오기 ---
openpyxl>=3.1

# --- Windows 전용 ---
pywin32>=305; sys_platform == "win32"

# --- 선택(주석 해제 시 사용) ---
# easyocr>=1.7             # GPU 가능 시 인식 정확도 향상, 설치 무거움
# python-Levenshtein       # 덱명 퍼지매칭 가속 (없으면 표준 difflib 사용)
